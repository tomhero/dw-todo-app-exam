import React from 'react';

export type TodoItemProps = {
  id?: string;
  className?: string;
};

const TodoItem = ({ id, className }: TodoItemProps) => {
  // TODO : Implementation
  return <div>TodoItem...</div>;
};

export default TodoItem;
